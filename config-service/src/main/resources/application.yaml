server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: Eureka
          uri: https://github.com/davidz12/microservicios
          search-paths: config-data
  application:
    name: config-service